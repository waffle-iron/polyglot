version: '2'
services:
  web:
    image: node:latest
    volumes:
      - ./:/usr/src/app
    working_dir: /usr/src/app
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    command: bash -c "npm install -g knex && knex migrate:latest --env production && npm start"
  postgres:
    image: postgres
    environment:
      - DB_NAME=lango
